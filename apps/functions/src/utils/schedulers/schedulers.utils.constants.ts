export const LOG_GROUP = 'SchedulersUtils';
export const SCHEDULE_DEFAULT_OPTIONS = {
  timeZone: 'America/Santiago',
};
export const LOGS = {
  SCHEDULER_STARTED: {
    logMessage: 'Scheduler started',
    logId: 'scheduler-started',
  },
};

export const STEPS = {
  SCHEDULER_STARTED: { label: 'scheduler-started' },
};